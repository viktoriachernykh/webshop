// import api from "../../api";

// export function fetchDevelopers(dispatch, getState) {
//   // export function fetchDevelopers(dispatch, getState??) { // instead of line 3 doesn't work
//   //   return (dispatch, getState) => { // instead of line 3 doesn't work
//   api("/developers").then(data => {
//     dispatch(developersFetched(data));
//   });
// }

// export function developersFetched(data) {
//   return {
//     type: "developers/FETCHED",
//     payload: data
//   };
// }
